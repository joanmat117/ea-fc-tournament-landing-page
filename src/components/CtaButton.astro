---
import {getTranslation} from '../i18n/utils'
const t = await getTranslation(Astro.currentLocale)
---

<button class={"bg-green-500 flex border-3 border-green-100/20 px-6 py-2 shadow-lg transition-all hover:scale-105 hover:tracking-wider text-lg cursor-pointer shadow-green-400/60 font-extrabold text-emerald-50 rounded-full "+Astro.props.class} >
  {t.hero.ctaButton}
</button>
